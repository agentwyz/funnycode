const std = @import("std");
const fs = std.fs;


pub fn main() anyerror!void {
    // var i: i32 = 14;
    // while (i < 18) : (i += 1) {
    //     //14, 
    //     std.debug.print("{d}\n", .{i});
    // }

    var file = fs.File.open
}
