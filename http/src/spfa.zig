const Eadge = struct {
    a i32,
    b i32,
    c i32,
};

//crate a instance
var eadges [N]Eadge = undefined;
const N = 510;
const M = 10000;


fn bf() {
    var i: i32 = 0;
    while (i < k): (i += 1) {
        for () || {
            dist[b] = min(dist[b], dist[a].c);
        }
    }
}

pub 
fn main() !void {

}

